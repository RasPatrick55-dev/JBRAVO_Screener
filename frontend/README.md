# Frontend (React)

## Build base path

The React dashboard is served from the site root (`/`).

When building for production, ensure the base path is `/` so that
`frontend/dist/index.html` references assets under `/assets/`.
